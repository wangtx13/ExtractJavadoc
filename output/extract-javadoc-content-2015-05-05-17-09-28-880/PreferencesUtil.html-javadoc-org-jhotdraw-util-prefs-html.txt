lang Object org jhotdraw util prefs Preferences Util Preferences Util install Frame Prefs Handler util prefs Preferences prefs lang String awt Window window install Palette Prefs Handler util prefs Preferences prefs lang String awt Window window install Palette Prefs Handler util prefs Preferences prefs lang String awt Window window install Internal Frame Prefs Handler util prefs Preferences prefs lang String javax swing J Internal Frame window javax swing J Desktop Pane desktop install Tool Bar Prefs Handler util prefs Preferences prefs lang String javax swing J Tool Bar toolbar Preferences Util lang Object Installs frame preferences handler On first run sets the window to its preferred size the top left corner of the screen On subsequent runs sets the window the last size and location where the user had placed it before Parameters prefs - Preferences for storing/retrieving preferences values - Base of the preference window - The window for which to track preferences Installs palette preferences handler On first run sets the palette to its preferred location the top left corner of the screen On subsequent runs sets the palette the last location where the user had placed it before Parameters prefs - Preferences for storing/retrieving preferences values - Base of the preference window - The window for which to track preferences Installs intenal frame preferences handler On first run sets the frame to its preferred size the top left corner of the desktop pane On subsequent runs sets the frame the last size and location where the user had placed it before Parameters prefs - Preferences for storing/retrieving preferences values - Base of the preference window - The window for which to track preferences Installs toolbar preferences handler On first run sets the toolbar to Border Layout T O P On subsequent runs set the toolbar to the last Border Layout location Parameters prefs - Preferences for storing/retrieving preferences values - Base of the preference toolbar - The J Tool Bar for which to track preferences 