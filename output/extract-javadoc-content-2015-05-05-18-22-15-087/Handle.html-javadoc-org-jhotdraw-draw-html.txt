Handle Figure get Owner set View Drawing View view add Handle Listener Handle Listener remove Handle Listener Handle Listener awt Rectangle get Bounds awt Rectangle get Draw Bounds contains awt Point draw awt Graphics2 invalidate dispose awt Cursor get Cursor Combinable With Handle handle track Start awt Point anchor modifiers Ex track Step awt Point anchor awt Point modifiers Ex track End awt Point anchor awt Point modifiers Ex track Click awt Point modifiers Ex view Transform Changed util Collection create Secondary Handles All Superinterfaces util Event Listener awt event Key Listener All Known Implementing Classes Abstract Handle Bezier Control Point Handle Bezier Node Handle Bezier Scale Handle Change Connection End Handle Change Connection Handle Change Connection Start Handle Close Handle Connection Handle Font Size Handle Locator Handle Move Handle Null Handle Rotate Handle Round Rect Radius Handle Triangle Rotation Handler Handle awt event Key Listener Returns the owner of this handle Sets the view of the handle Adds listener for this handle Removes listener for this handle Returns the bounding box of the handle Returns the draw bounds of the handle Tests if point contained the handle Draws this handle Invalidates the handle This informs its listeners that its current display box invalid and should be refreshed Disposes the resources aquired by the handler Returns cursor for the handle Returns if this handle combinable with the specified handle This used to determine if multiple handles need to be tracked when more than one figure selected Tracks the start of the interaction The default does nothing Parameters anchor - the position where the interaction started Tracks step of the interaction Parameters anchor - the position where the interaction started - the current position Tracks the of the interaction Parameters anchor - the position where the interaction started - the current position Tracks click This invoked by the drawing view when its transform has changed This means that Drawing View view To Drawing and Drawing View drawing To View will different values than they did before Creates secondary handles 